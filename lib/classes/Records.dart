class Records {
  String userName,
      recordID,
      recordType,
      recordDecs,
      recordDate,
      recordTime,
      itemID,
      description,
      onayDurum,
      ysorumlu;

      bool isSelected = false;

  Records(
      this.userName,
      this.recordID,
      this.recordType,
      this.recordDecs,
      this.recordDate,
      this.recordTime,
      this.itemID,
      this.description,
      this.onayDurum,
      this.ysorumlu);
}